
function checarSePodeDirigir () { 
    let idade = prompt ('Qual sua idade?');
    
    if (idade > 18) { 
        alert ('Pode dirigir!')
     }  else {
         alert ('Ainda não pode dirigir!')
     }
    }

    
checarSePodeDirigir (17); 
